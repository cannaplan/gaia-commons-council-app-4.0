# Environment Variables

# PostgreSQL connection string (required for the server and Drizzle ORM)
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Server port (optional, defaults to 5000)
PORT=5000

# Node environment (development | production)
NODE_ENV=development

# ---------------------------------------------------------------------------
# Deployment variables (required in production / GitHub Actions secrets)
# ---------------------------------------------------------------------------

# Public URL where the application is deployed (used for CORS, redirects, etc.)
DEPLOY_URL=https://your-app.example.com

# Production PostgreSQL connection string (set as a GitHub Actions secret:
# PRODUCTION_DATABASE_URL).  May differ from the development DATABASE_URL.
PRODUCTION_DATABASE_URL=postgresql://user:password@prod-host:5432/dbname
