import React from "react";

/**
 * Minimal placeholder for GreenhousePreview to unblock builds.
 * Replace with the real component implementation later.
 */
export default function GreenhousePreview(): JSX.Element {
  return (
    <section style={{ padding: 12 }}>
      <h2 style={{ margin: 0 }}>Greenhouse Preview</h2>
      <p style={{ marginTop: 8 }}>Placeholder component — replace with real Greenhouse widget.</p>
    </section>
  );
}
